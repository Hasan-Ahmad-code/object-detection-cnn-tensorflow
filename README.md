# object-detection-cnn-tensorflow
his repository implements a simple object detection model using TensorFlow and Keras. It detects three classes: car, dog, and person, and predicts bounding boxes. The dataset was annotated with Roboflow and exported in Pascal VOC XML. MobileNetV2 is used for feature extraction while the model performs classification and box regression.
